# Copy to .env for local dev, or set in Netlify env vars for production.
# The base URL of your backend API (Oracle server).
# Local dev default:
VITE_API_URL=http://localhost:8000

# Production example:
# VITE_API_URL=https://your-oracle-server-domain-or-ip:8000
